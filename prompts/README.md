# Prompts

Prompt templates are versioned in this directory.

Milestone 2 uses `structure-evidence.v1.md` for synchronous evidence structuring. Future milestones must keep adding prompt files here instead of embedding prompt text inline in route handlers.
